version: '3'

services:
  web:
    image: nginx:1.21.3
    ports:
      - '3001:80'
    volumes:
      - ./src:/var/www/docker-demo
      - ./virtual-host.conf:/etc/nginx/conf.d/default.conf
    links:
      - php
  php:
    image: php:8.0-fpm
    volumes:
      - ./src:/var/www/docker-demo
